import 'package:flutter/material.dart';

class AppColors {
  // Primary — matches website #2563eb (blue-600)
  static const primary = Color(0xFF2563EB);
  static const primaryDark = Color(0xFF1D4ED8);
  static const primaryLight = Color(0xFFEFF6FF);
  static const primaryRing = Color(0xFFBFDBFE);

  // Gradient
  static const gradientStart = Color(0xFF2563EB);
  static const gradientEnd = Color(0xFF6366F1);

  // Backgrounds — matches #f8fafc / #f1f5f9
  static const background = Color(0xFFF8FAFC);
  static const backgroundAlt = Color(0xFFF1F5F9);
  static const surface = Colors.white;
  static const surfaceAlt = Color(0xFFF8FAFC);

  // Borders — matches #e2e8f0
  static const border = Color(0xFFE2E8F0);
  static const borderLight = Color(0xFFF1F5F9);

  // Text — matches #0f172a, #64748b, #94a3b8
  static const textPrimary = Color(0xFF0F172A);
  static const textSecondary = Color(0xFF64748B);
  static const textMuted = Color(0xFF94A3B8);
  static const textLight = Color(0xFFCBD5E1);

  // File type accent colors — matching website legend
  static const violet = Color(0xFF8B5CF6);
  static const violetLight = Color(0xFFF3E8FF);
  static const violetRing = Color(0xFFEDE9FE);

  static const amber = Color(0xFFF59E0B);
  static const amberLight = Color(0xFFFEF3C7);

  static const red = Color(0xFFEF4444);
  static const redLight = Color(0xFFFEF2F2);

  static const blue = Color(0xFF3B82F6);
  static const blueLight = Color(0xFFEFF6FF);

  static const green = Color(0xFF10B981);
  static const greenLight = Color(0xFFECFDF5);

  // Stat card backgrounds
  static const statBlue = Color(0xFFEFF6FF);
  static const statViolet = Color(0xFFF5F3FF);
  static const statEmerald = Color(0xFFECFDF5);
  static const statAmber = Color(0xFFFFFBEB);

  // Icon backgrounds for stat cards
  static const iconBlue = Color(0xFF2563EB);
  static const iconViolet = Color(0xFF7C3AED);
  static const iconEmerald = Color(0xFF10B981);
  static const iconAmber = Color(0xFFF59E0B);

  // Danger / error
  static const danger = Color(0xFFEF4444);
  static const dangerLight = Color(0xFFFEF2F2);
  static const dangerBorder = Color(0xFFFECACA);

  // Success
  static const success = Color(0xFF10B981);
  static const successLight = Color(0xFFECFDF5);

  // Shadow colors
  static const shadowBlue = Color(0x3F2563EB);
  static const shadowMd = Color(0x1A000000);
}
